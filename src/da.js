import React, {
    useState
} from 'react';

const App = () => {
    const [change, setChange] = useState(false);
    const [list, setList] = useState(data)
    const generateValue = () => {
        return Math.round(100 + Math.random() * 900);
    }
    let size = 200
    cosnt data = Array.from({
        length: size ? ? 200
      }, (_el, index) => ({
        label: label $ {
            index + 1
        },
    value: App.generateValue()
      })),
console.log('data', data)

return (
    < >
    </ >)
  };
export default App;
